# DevConnect — Master Task Table

Generated Phase 0, 2026-07-17. Sorted by priority, then phase.
Status values: `Not started` · `In progress` · `Done` · `Deferred (reason)`

Effort: **S** ≤1h · **M** ~half a day · **L** ~1–2 days

| # | Phase | Task | Category | Priority | Effort | Status | Files affected |
|---|---|---|---|---|---|---|---|
| 1 | 2 | **Adopt a real migrations baseline.** ~~No `prisma/migrations/` exists~~ → created `0_init` (23 tables, 9 enums, 37 indexes, 33 FKs) incl. the BUG-01/BUG-05 security columns. Confirmed zero drift on the dev DB, then `migrate resolve --applied 0_init`. **Verified** on a throwaway Neon DB: `migrate deploy` on an empty database → byte-exact schema match. Also added `directUrl` (Neon pooler breaks `migrate`'s advisory locks) + `migration_lock.toml`. | DB | **Critical** | M | **Done** | `prisma/migrations/0_init/migration.sql`, `prisma/migrations/migration_lock.toml`, `prisma/schema.prisma`, `.env.example` |
| 2 | 4 | **Env validation on boot.** Added Zod-validated `lib/config.ts`: parses all env once at startup, exits 1 listing every problem (names only, never values). Enforces `JWT_SECRET` ≥32; in prod requires `CLIENT_URL`/`SERVER_URL`/`DIRECT_DATABASE_URL`, rejects localhost, validates each CORS origin, requires OAuth pairs to be set together, and **requires ≥1 OAuth provider** (see #30). Treats `FOO=""` as unset. Migrated all 15 vars off scattered `process.env` reads. **Verified**: 9 boot scenarios. | Prod/Security | **Critical** | S | **Done** | `src/lib/config.ts` (new), `src/index.ts`, `src/lib/{jwt,cors,email,prisma}.ts`, `src/routes/{auth,profiles}.ts`, `src/socket.ts`, `.env.example` |
| 3 | 1 | **BUG-06 — brute-force protection was IP-only.** (a) `trust proxy: 1` was hardcoded → now explicit `TRUST_PROXY` config (#2). (b) IP limiting can't stop a **distributed** attack — empirically proven: 25 logins from 25 IPs, **0 blocked**. Added DB-backed per-account throttling (20 failures/15min → 15min lock), which survives restarts unlike an in-memory counter. Threshold sits *above* the IP limit (10) so real users hit the clear IP message first; the lock returns generic `Invalid credentials` to avoid becoming an enumeration oracle. Reset clears the lock so a victim isn't stuck. **Verified**: `login-throttle.spec.ts` 8/8, confirmed to report VULN when the check is disabled. | Security | **Critical** | M | **Done** | `prisma/schema.prisma`, `migrations/20260717044540_add_login_throttling`, `src/lib/loginThrottle.ts`, `src/routes/auth.ts`, `src/tests/login-throttle.spec.ts` |
| 4 | 1 | **Rate limiters are in-memory.** MemoryStore resets on restart and is per-instance. **Deferred — revisited in Phase 1:** at 1 VPS / 1 instance the per-instance problem doesn't exist, and the restart-reset is now covered by the DB-backed account lock (task 3), which is what actually stops distributed attacks. Adding Redis solely for this buys a service to secure and monitor against a problem that is half absent. Revisit when going multi-instance. | Security/Perf | **Low** | S | **Deferred (see task 9)** | `src/middleware/rateLimit.ts` |
| 5 | 4 | **No test runner.** Was: hand-rolled `tsx` scripts needing a live server on a fixed port, unusable in CI, with nothing guarding the fixed BUG-01…05. Now **Vitest + Supertest, 91 tests across 8 files, no server required** — `index.ts` was split into `app.ts` (builds the app) + `index.ts` (listen/socket/shutdown) so tests mount the app in-process. `security.spec.ts` ported first as asked, covering BUG-01…05 + JWT integrity. `socket-broadcast` keeps a real server (socket.io needs a live connection) but on an **ephemeral** port. **`TEST_DATABASE_URL` is required with no fallback** — the suite mutates rows, and falling back to `DATABASE_URL` is exactly how a test run writes to production; setup also forces `EMAIL_DISABLED=1`. `fileParallelism: false` is a correctness requirement (shared DB + global rate-limit/lock state), documented in `src/tests/README.md`. **Verified**: 91/91 pass; negative controls disabling the admin guard, `no-store`, the Cloudinary host check and the `discoverable` filter produced 12 failures across 4 files. Those controls also caught **two of my own tests passing for the wrong reason** — a talent-search check that only looked at page 1 (so it stayed green with the filter removed) and a wrong 403 expectation that would have had me "fix" correct code. **Remaining `.spec.ts` documented with reasons** in the tests README (4 legacy feature scripts; timing + shutdown stay manual by design). | Prod | **High** | L | **Done** | `vitest.config.ts`, `src/app.ts` (new), `src/index.ts`, `src/tests/{setup,helpers}.ts`, `src/tests/*.test.ts` (8), `src/tests/README.md`, `package.json` |
| 6 | 4 | **Dockerfile + docker-compose** (app + postgres + redis), multi-stage build, non-root user, `.dockerignore`. Prerequisite for a reproducible VPS deploy. | Prod | **High** | M | Not started | `server/Dockerfile`, `docker-compose.yml`, `.dockerignore` (all new) |
| 7 | 4 | **DEPLOYMENT.md** — exact fresh-VPS runbook: install, secrets, migrate, run, nginx reverse proxy, TLS via certbot, DNS, backups. The stated end goal of this engagement. | Prod | **High** | M | Not started | `DEPLOYMENT.md` (new) |
| 8 | 1 | **Token lifetime & revocation.** Was: 7d/30d JWTs in `localStorage`, no refresh, revocable only by a global `tokenVersion` bump (signs out every device), no server-side logout, no per-device revocation — a stolen token meant full access for a month. Now: **15-minute access token held in memory only** (XSS can no longer steal a persistent session) + **DB-backed refresh token in an httpOnly cookie**, with rotation, **reuse detection** (a replayed token means two copies exist → revoke the whole family), per-device sessions (`GET/DELETE /api/auth/sessions`), real server-side `logout`, and `logout-all`. **Password reset now revokes refresh tokens too** — without that, `tokenVersion` only killed access tokens and an attacker's refresh token kept working *after* the victim reset their password, defeating the point of a reset. A **15s grace window** on rotation stops two tabs refreshing at once from looking like theft and logging the user out. CSRF on the cookie routes is blocked by requiring a custom header (forces a preflight). **Verified**: `refresh.spec.ts` 23/23 + negative controls on both reuse-detection and reset-eviction; and driven in a real browser — login → `localStorage` holds no token, `document.cookie` is empty, reload restores the session via `/refresh` alone. ⚠️ **See #31.** | Security | **High** | L | **Done** | `src/lib/{jwt,refreshTokens,refreshCookie}.ts`, `src/routes/auth.ts`, `migrations/20260717162822_add_refresh_tokens`, `prisma/schema.prisma`, `client/src/lib/{token,api,auth,socket}.ts(x)`, `client/src/pages/AuthCallback.tsx`, `src/tests/refresh.spec.ts` |
| 31 | 4 | **⚠️ The API must be same-site with the client (YOU must decide).** The refresh cookie is `SameSite=None` in production, which works for both topologies — but the client is on **Vercel** and the API on **Render**, i.e. *different sites*, so the cookie is **third-party**. Safari blocks those by default and Chrome is heading the same way: those users would be signed out every 15 minutes. This isn't fixable with cookie flags. Fix: serve the API from `api.your-domain.com` alongside the client on `your-domain.com` — which is what the planned VPS + domain deploy does anyway. Until then, prefer testing in Chrome/Firefox. | Security/Prod | **High** | S | **Blocked — needs your action** | `DEPLOYMENT.md`, DNS, `CLIENT_URL`/`SERVER_URL` |
| 9 | 2 | **Add Redis** (cache + rate-limit store + socket adapter). **Deferred — revisited in Phase 1:** premature for a single-VPS MVP. The `tokenVersion` lookup is an indexed primary-key read Neon handles fine at this scale, and rate limiting is covered by task 3. Redis becomes the right call at multi-instance (shared limiter + socket adapter) — that trigger and the full reasoning become the explicit "do I need another database?" verdict in `DATABASE.md` (task 17). | DB/Perf | **Low** | M | **Deferred (documented in task 17)** | `DATABASE.md` |
| 10 | 3 | **BUG-09 — feed loads ≤600 rows/request** and merges/sorts in JS with offset paging (drops/duplicates items as posts arrive). Move to keyset pagination on an indexed sort key. | Perf | **High** | L | Not started | `src/routes/posts.ts`, `src/schemas/posts.ts`, `prisma/schema.prisma` |
| 11 | 4 | **Health/readiness/liveness.** `/api/health` was static JSON that never touched the DB, so a load balancer would route traffic to an instance with a dead database. Added `/api/readyz` (checks DB, 503 when down or draining) and `/api/livez` (no dependency checks — restarting can't fix a dead DB, so checking it there causes crash loops). Registered **before** `apiLimiter`: LB polling every 2s would otherwise exhaust 300/15min and get 429'd out of service while healthy. DB check cached 2s since the route sits ahead of the limiter. **Verified**: 320-req burst → exempt (control: normal route 429s); real unreachable DB → `livez` 200 / `readyz` 503. | Prod | **High** | S | **Done** | `src/routes/health.ts` (new), `src/index.ts` |
| 12 | 4 | **Graceful shutdown + request timeouts.** Added SIGTERM/SIGINT drain: readiness→503, 3s LB-notice delay, close sockets+HTTP, disconnect Prisma, 15s force-exit backstop. Added `headersTimeout` 30s / `requestTimeout` 60s (slowloris; kept above socket.io's 25s long-poll window) and `keepAliveTimeout` 80s (above nginx's 75s, else random 502s). **A test caught a real bug**: `io.close()` already closes the HTTP server, so the following `httpServer.close()` threw `ERR_SERVER_NOT_RUNNING` → every deploy would exit **1** and look like a crash. **Verified**: `shutdown.spec.ts` 5/5. ⚠️ Signal *delivery* unverifiable on Windows (no POSIX signals) — drain *logic* is tested; confirm on Linux at deploy (#7). | Prod | **High** | S | **Done** | `src/index.ts`, `src/tests/shutdown.spec.ts` |
| 13 | 1 | **BUG-10 — Socket.io broadcasts leaked to every connected client.** `broadcastPostUpdate`/`broadcastProfileUpdate` used global `io.emit`, so every connected user received post ids + engagement counts for **private communities** they cannot read — the guarantee BUG-02 fixed in REST, leaking via real-time. Now scoped: public → all; private community → member rooms only; `profile:update` excludes blocked users (closes a BUG-04 gap). `communityId` is a **required** param so new call sites must decide the audience. Costs **0 extra queries** (folded into existing selects). **Verified**: new `socket-broadcast.spec.ts`, 7/7 pass — and confirmed the test reports VULN when the fix is reverted. | Security | **High** | M | **Done** | `src/socket.ts`, `src/routes/{posts,friends}.ts`, `src/tests/socket-broadcast.spec.ts` |
| 14 | 4 | **CI pipeline** — GitHub Actions, two jobs. **Server**: install → typecheck → `prisma migrate deploy` against a throwaway **Postgres 16 service container** → 91-test Vitest suite → build → production `npm audit`. **Client**: typecheck → build → audit. Node pinned to **22** (matches `engines`/deploy target though local dev is 26, so a version gap surfaces here — part of #35). `npm audit` scoped `--omit=dev` so dev-only advisories (client `vite`, #34) don't gate the build. **Verified on the real runner** (PR #20): both jobs green, and confirmed the suite genuinely ran — **91/91 against vanilla Postgres, not Neon**, which was the one thing I couldn't prove locally; migrations applied from empty, audits clean. Lint (ESLint/Prettier) is its own task **#26**, not folded in here. | Prod | **Med** | S | **Done** | `.github/workflows/ci.yml` (new), `client/.gitignore` |
| 15 | 1 | **Dependency & headers review.** Full write-up in **`SECURITY_HEADERS.md`** — every header captured from a live response, not read off docs. Server `npm audit`: **0 vulns**. Client: 4 advisories, all in `vite` (devDependency) and all **dev-server only**, so none reach production — left unfixed on purpose since the only remedy is `vite` 5→8 (`isSemVerMajor`), which is a build-toolchain change needing its own regression pass (**#34**); one of them leaks NTLMv2 hashes on Windows, which matters to you locally. Verified CORS **rejects** unknown origins rather than reflecting them — the property the `/refresh` CSRF defence depends on. **Two real gaps found and fixed:** (1) **no `Cache-Control`** anywhere, so browsers applied *heuristic* caching to `/api/auth/me` — email/username/profile written to disk cache, readable after logout or by another user of the machine; now `no-store` on all `/api`. (2) An oversized or malformed body returned **500 instead of 413/400** — client errors were being reported as server faults and would have flooded error monitoring; `errorHandler` now maps body-parser errors, guarded by a `type` check so it won't trust `.status` on arbitrary errors. Reviewed-and-kept (with reasoning recorded): CORP `same-origin`, HSTS without `preload`, CSP on JSON, `X-Frame-Options: SAMEORIGIN`. **Verified**: `headers-cors.spec.ts` 17/17; negative control (reflecting CORS + `no-store` removed) reported 4 VULNs. | Security | **Med** | S | **Done** | `SECURITY_HEADERS.md`, `src/index.ts`, `src/middleware/errorHandler.ts`, `src/tests/headers-cors.spec.ts` |
| 34 | 1 | **Upgrade client `vite` 5 → 8.** Clears all 4 client advisories (see `SECURITY_HEADERS.md`). Dev-server only, so no production exposure — but the launch-editor one discloses **NTLMv2 hashes on Windows**, which is your dev machine. Three major versions with breaking config changes; needs its own branch plus a full client build/regression pass, which is why it wasn't folded into a backend security commit. | Security | **Med** | M | Not started — **needs your decision** | `client/package.json`, `client/vite.config.ts` |
| 35 | 4 | **Node version mismatch.** `server/package.json` pins `"engines": { "node": "22.x" }` but the local runtime is **v26.5.0**, and `engines` is advisory without `engine-strict`. You develop on 26 and would deploy on 22, so version-specific behaviour wouldn't surface until production. Pick one target, set it in `.nvmrc` + Docker base image + CI, and enable `engine-strict` so the mismatch fails loudly instead of silently. | Prod | **Med** | S | Not started — **needs your decision** | `server/package.json`, `.nvmrc`, `Dockerfile`, CI |
| 16 | 2 | **Caching strategy.** Cache the per-request `tokenVersion` lookup (currently 1 DB read per authenticated request *and* per socket handshake), plus feed sidebar and talent facets. | Perf/DB | **Med** | M | Not started | `src/middleware/auth.ts`, `src/lib/redis.ts`, `src/routes/{feed,talent}.ts` |
| 17 | 2 | **DATABASE.md** + the explicit "do I need another database?" verdict, and a documented (not implemented) replica/partitioning path. **Revised by Phase 1 discovery:** the DB is **Neon** (managed, pooled) — so pooling is already solved and the VPS does *not* need to run Postgres. Needs `connection_limit` tuned against Neon's ceiling instead. | DB | **Med** | M | Not started | `DATABASE.md` (new), `src/lib/prisma.ts`, `.env.example` |
| 18 | 3 | **Load testing with k6** — scripts for signup, login, create post, load feed, follow, send message; capture RPS + p50/p95/p99 + error rate before and after #10/#16. | Perf | **Med** | L | Not started | `load/**` (new), `LOAD_TEST_REPORT.md` |
| 19 | 4 | **Structured logging + error tracking.** `console.log`/`console.error` only, no request ids, no correlation. Add pino + a Sentry hook, with redaction so tokens/passwords never reach logs. | Prod | **Med** | M | Not started | `src/lib/logger.ts` (new), `src/middleware/errorHandler.ts`, `src/index.ts` |
| 20 | 1 | **BUG-07 — forgot-password timing enumeration.** Body was identical for existing vs non-existing emails, but the existing branch did a DB write + SMTP send *before responding* → the response time alone let an attacker enumerate registered emails. Fixed: respond first, then run the lookup + write + email in a detached background task (`sendResetEmail`, errors logged only — it can't touch the sent response). Timing is now constant. **Verified**: `forgot-password-timing.spec.ts` 3/3 (median gap 0.1ms); the negative control (work awaited before response + a 200ms simulated SMTP delay) reported VULN at a 2332ms gap — and building that control caught a **real test bug first**: a per-run IP tripped the 10/IP `authLimiter` after 10 requests, so most samples were uniform 429s that washed out the signal (fixed to a unique IP per request). | Security | **Med** | S | **Done** | `src/routes/auth.ts`, `src/tests/forgot-password-timing.spec.ts` |
| 21 | 1 | **BUG-11 — upload URLs accepted any host.** Uploads go client-side to Cloudinary and the API only stored the URL, validated as `http(s)` only — so a user could point `avatarUrl`/`imageUrl`/attachment at a server they control, and every viewer's browser would fetch it (IP leak + tracking), bypassing Cloudinary's size limits and moderation entirely. Added `cloudinaryUrl()`: host must be `res.cloudinary.com`, and when `CLOUDINARY_CLOUD_NAME` is set the first path segment must match **our** account. Applied to the upload-backed fields only (`avatarUrl`, `bannerUrl`, `resumeUrl`, post `imageUrl`, message `attachmentUrl`); `websiteUrl`/`githubUrl` stay arbitrary `http(s)` on purpose — they're legitimate external links, and restricting them would break the feature. Unset cloud name falls back to host-only (dev) with a prod boot warning rather than a hard fail, so an existing deploy can't be bricked. **Verified**: `upload-host.spec.ts` 11/11 incl. look-alike host (`res.cloudinary.com.evil.com`) and subdomain (`evilres.cloudinary.com`) tricks; negative control reported 4 VULNs with the host check disabled; cloud-name tie confirmed live (our cloud 200, another account's 422). **Scope note:** this closes the *host* half of the original task; server-side type/size enforcement is split out as #32 because it needs an architecture decision, not just validation. | Security | **Med** | M | **Done** | `src/schemas/{profile,posts,auth}.ts`, `src/socket.ts`, `src/lib/config.ts`, `.env.example`, `src/tests/upload-host.spec.ts` |
| 22 | 2 | **Missing index for the global "recent" feed sort.** Every `Post` index leads with a discriminator (`authorId`/`type`/`communityId`), so an unfiltered `ORDER BY createdAt DESC` has no usable index. | DB/Perf | **Med** | S | Not started | `prisma/schema.prisma`, migration |
| 23 | 4 | **OpenAPI/Swagger spec** for all 68 endpoints, served at `/api/docs` in non-prod. | Prod | **Med** | L | Not started | `src/lib/openapi.ts` (new), `src/index.ts` |
| 24 | 1 | **BUG-08 — offline-email subject unescaped.** `displayName` flowed raw into the email subject while the body escaped it. The subject is a **header**, so CR/LF in a display name can inject `Bcc:`/`Reply-To:` into mail sent under our name. **The original "low impact — nodemailer rejects header newlines" note was too generous**: OAuth display names (`gh.name`/`g.name` in `routes/auth.ts`) are stored **without passing any Zod schema**, so a schema-only fix would have missed the one path an attacker controls. Fixed at two layers: (1) `headerSafe()` inside `sendEmail` — the single point every email funnels through, so no future caller can forget; (2) `displayName` schema now rejects control characters at the source. Deliberately **not** HTML-escaped — a subject is plain text, so escaping would render `&amp;` literally to users; RTL marks are preserved since they're legitimate in Arabic/Hebrew names. **Verified**: `email-header.spec.ts` 9/9; negative control with both transforms disabled reported 4 VULNs. That control also corrected my own model of the code — CR/LF are actually neutralised by the whitespace collapse, while `\p{Cc}` covers non-whitespace control chars; the redundancy is now documented. | Security | **Low** | S | **Done** | `src/lib/email.ts`, `src/schemas/{profile,auth}.ts`, `src/socket.ts`, `src/tests/email-header.spec.ts` |
| 33 | 4 | **`EMAIL_DISABLED` guard for local runs.** Found the hard way while verifying #24: the security suites hammer email-sending endpoints (`forgot-password` ~50×/run), and with real SMTP credentials in `.env` those were sending **real mail to non-existent addresses** — burning send quota and generating bounces that damage sender reputation. Added `EMAIL_DISABLED=1` to force the console path regardless of SMTP config, documented in `.env.example` and in the header of every spec that triggers mail. | Prod | **Med** | S | **Done** | `src/lib/config.ts`, `.env.example`, `src/tests/forgot-password-timing.spec.ts` |
| 25 | 1 | **Reports were write-only.** `POST /api/moderation/report` accumulated rows with no `status`, no reviewer surface, and no admin role — moderation was non-functional. Added `ReportStatus` + `reviewedAt`/`reviewedById`/`resolutionNote`, and `/api/admin/reports` (queue, stats, detail, PATCH review). **Admin is `User.isAdmin`, deliberately *not* a `Role` value** — `Role` is the account type (`requireRole("RECRUITER")` builds on it), so an ADMIN enum member would stop an admin being a DEVELOPER/RECRUITER and break those checks. `isAdmin` is read **from the DB per request, not from the JWT**, so revocation is instant and needs no `tokenVersion` bump — at **0 extra queries** (folded into the existing `tokenVersion` select). `requireAdmin` returns **404 not 403**, so the route doesn't confirm its own existence to non-admins. First admin is granted via `npm run admin:grant` (a script, not an endpoint — any "bootstrap first admin" endpoint is a backdoor). **Verified**: `admin.spec.ts` 17/17, and confirmed it reports VULN when the guard is disabled. **API only — dashboard UI is yours to build later.** | Security | **Low** | L | **Done** | `prisma/schema.prisma`, `migrations/20260717160524_add_report_review_and_admin`, `src/routes/admin.ts`, `src/middleware/auth.ts`, `scripts/admin.ts`, `src/tests/admin.spec.ts`, `src/index.ts`, `package.json` |
| 26 | 4 | **Lint/format** — ESLint + Prettier, wired into CI (#14). | Prod | **Low** | S | Not started | `.eslintrc`, `.prettierrc` (new), `server/package.json` |
| 27 | 4 | **Backups** — documented `pg_dump` schedule + a tested restore procedure. | Prod | **Low** | S | Not started | `DEPLOYMENT.md` |
| 28 | 1 | **No account deletion / data export.** Cascade deletes are correctly declared throughout, but no endpoint exercises them — a GDPR-shaped gap if you take EU users. | Security | **Low** | M | Not started | `src/routes/profiles.ts` |
| 30 | 4 | **⚠️ Register GitHub + Google OAuth apps for production (YOU must do this).** Found in Phase 1: email/password sign-up is hard-restricted to `@devconnect.com` (`schemas/auth.ts`, deliberate — commit `5a815d3`), so **OAuth is the only way the public can create an account**. Both client IDs are currently empty. Without them, launch day has *zero* public signup path. Needs OAuth apps whose callback URLs point at the production `SERVER_URL`. Config now refuses to boot in prod without ≥1 provider, so this can't be missed silently. | Prod | **Critical** | S | **Blocked — needs your action** | `.env` (prod), GitHub/Google consoles, `DEPLOYMENT.md` |
| 32 | 1 | **Upload type/size enforcement (split out of #21).** #21 proved a stored URL points at our Cloudinary account, but **not** what's behind it. The server never sees the file — the browser uploads straight to Cloudinary with an **unsigned** preset (`VITE_CLOUDINARY_UPLOAD_PRESET`), so today any authenticated user can push arbitrary files of arbitrary size into your account and get a URL that passes validation. The client's `.pdf` check is cosmetic (client-side only). **Options:** (a) tighten the Cloudinary preset itself — allowed formats, max file size, max image dimensions, auto-moderation — which is console config, no code, and fixes the bulk of it; (b) switch to **signed** uploads so the server authorises each one and controls the constraints; (c) proxy uploads through the API (most control, most cost/complexity — not recommended at this stage). **Recommended default: (a) now, (b) when you have time.** Left undone rather than half-built because it's a deployment/architecture choice, not a code gap. | Security | **Med** | M | Not started — **needs your decision** | Cloudinary console, `client/src/components/{ImageUpload,FileUpload,Composer}.tsx`, `DEPLOYMENT.md` |
| 29 | 1 | **`bcryptjs` → `bcrypt` or `argon2`.** `bcryptjs` is the pure-JS implementation: ~3–5× slower than native, so cost 12 costs *you* more CPU per login than it costs an attacker with native code. Cost 12 is adequate today; argon2id is the current recommendation. | Security | **Low** | S | Not started | `server/package.json`, `src/routes/auth.ts` |

## Summary by priority
| Priority | Count |
|---|---|
| Critical | 4 |
| High | 11 |
| Med | 14 |
| Low | 6 |
| **Total** | **35** |

## Status
| Status | Count |
|---|---|
| Done | 15 |
| Deferred (with reason) | 2 |
| Blocked — needs your action | 2 |
| Not started | 17 |
